const comparator = require('./comparator/comparator');
const crawlerExceptions = require('./crawlerExceptions/crawlerExceptions');
const json = require('./json/json');

module.exports = {
  comparator,
  crawlerExceptions,
  json,
};
