const key = require('./key/key');
const value = require('./value/value');
const duplicated = require('./duplicated/duplicated');
const underscore = require('./underscore/underscore');
const __texts = require('./__texts/__texts');
const arrays = require('./arrays/arrays');
const installment = require('./installment/installment');

module.exports = {
  key,
  value,
  duplicated,
  underscore,
  __texts,
  arrays,
  installment,
};
